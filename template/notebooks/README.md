# Notebooks are demos only

Import Dataset / Model / Trainer classes from `src/`. Do not keep training loops or model definitions only in notebooks — promote finished logic into packages so projects stay testable and reusable.
